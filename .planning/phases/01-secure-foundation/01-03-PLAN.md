---
phase: 01-secure-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: ["package.json", "next.config.js", "tailwind.config.js", "src/app/layout.tsx", "src/app/page.tsx", "src/components/ProfileCard.tsx"]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Next.js web platform is properly configured and running"
    - "Profile display components render bot information correctly"
    - "Tailwind CSS styling system is set up for responsive design"
    - "Basic routing structure exists for bot profiles"
  artifacts:
    - path: "package.json"
      provides: "Next.js and frontend dependencies"
      contains: "next, react, react-dom, tailwindcss"
    - path: "next.config.js"
      provides: "Next.js configuration for bot profile platform"
      min_lines: 10
    - path: "src/app/layout.tsx"
      provides: "Root layout with Tailwind styling and metadata"
      min_lines: 20
    - path: "src/app/page.tsx"
      provides: "Homepage with bot profile introduction"
      min_lines: 30
    - path: "src/components/ProfileCard.tsx"
      provides: "Component for displaying bot profile information"
      min_lines: 40
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/ProfileCard.tsx"
      via: "ProfileCard component import and usage"
      pattern: "import.*ProfileCard"
    - from: "src/app/layout.tsx"
      to: "tailwind.config.js"
      via: "Tailwind CSS global styles"
      pattern: "@tailwind"
---

<objective>
Create Next.js web platform foundation with bot profile display components and responsive styling system.

Purpose: Establish the web platform foundation for displaying ClawdBot profiles with modern React components and Tailwind CSS styling, ready to display configuration data extracted by the CLI tool.
Output: Working Next.js application with profile display components and responsive design system.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-secure-foundation/01-DISCOVERY.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with TypeScript and Tailwind CSS</name>
  <files>package.json, next.config.js, tailwind.config.js</files>
  <action>
  1. Update package.json to include Next.js dependencies:
     - next@16.1.6, react@18, react-dom@18
     - tailwindcss@3.4.0 and supporting dependencies (autoprefixer, postcss)
     - TypeScript types for React (@types/react, @types/react-dom)
     - Build and development scripts
  
  2. Create next.config.js with:
     - App Router enabled (default for Next.js 16)
     - Static export configuration for profile pages
     - Image optimization settings for bot avatars
  
  3. Create tailwind.config.js with:
     - Content paths for TypeScript and React files
     - Custom color palette inspired by gaming aesthetics
     - Responsive breakpoints for mobile and desktop
     - Custom animations and transitions
  
  4. Create postcss.config.js for Tailwind processing
  5. Install all dependencies using npm
  
  From STACK.md research: Use Next.js 16.1.6 with App Router for hybrid rendering and Tailwind CSS 3.4+ for rapid UI development.
  </action>
  <verify>npm install completes successfully and Next.js configuration is valid</verify>
  <done>Next.js project initialized with TypeScript support and Tailwind CSS styling system</done>
</task>

<task type="auto">
  <name>Task 2: Create root layout and homepage structure</name>
  <files>src/app/layout.tsx, src/app/page.tsx</files>
  <action>
  1. Create src/app/layout.tsx with:
  
  ```typescript
  import type { Metadata } from 'next';
  import './globals.css';

  export const metadata: Metadata = {
    title: 'BotArena - AI Bot Showcase Platform',
    description: 'Showcase your AI agent configurations in a beautiful, comparable format',
    authors: [{ name: 'BotArena Team' }],
    viewport: 'width=device-width, initial-scale=1',
  };

  export default function RootLayout({
    children,
  }: {
    children: React.ReactNode;
  }) {
    return (
      <html lang="en">
        <body className="min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900">
          <div className="container mx-auto px-4 py-8">
            <header className="text-center mb-8">
              <h1 className="text-4xl md:text-6xl font-bold text-white mb-2">
                🤖 BotArena
              </h1>
              <p className="text-lg md:text-xl text-purple-200">
                Showcase Your AI Agent Configuration
              </p>
            </header>
            <main>
              {children}
            </main>
          </div>
        </body>
      </html>
    );
  }
  ```
  
  2. Create src/app/globals.css with Tailwind imports and custom styles
  
  3. Create src/app/page.tsx with homepage:
  
  ```typescript
  import { HeroSection } from '@/components/HeroSection';
  import { FeaturedProfiles } from '@/components/FeaturedProfiles';

  export default function HomePage() {
    return (
      <div className="space-y-12">
        <HeroSection />
        <FeaturedProfiles />
      </div>
    );
  }
  ```
  
  4. Add gaming-inspired visual design with gradient backgrounds and animations
  
  From STACK.md research: Use App Router for static generation of profile content and gaming-inspired visuals.
  </action>
  <verify>Next.js development server starts successfully and pages render without errors</verify>
  <done>Root layout and homepage created with gaming-inspired design and proper TypeScript support</done>
</task>

<task type="auto">
  <name>Task 3: Create bot profile display components</name>
  <files>src/components/ProfileCard.tsx, src/components/HeroSection.tsx, src/components/FeaturedProfiles.tsx</files>
  <action>
  1. Create src/components/ProfileCard.tsx:
  
  ```typescript
  'use client';

  import { PublicBotConfig } from '@/schemas/bot-config';

  interface ProfileCardProps {
    profile: PublicBotConfig;
    onViewProfile?: () => void;
  }

  export function ProfileCard({ profile, onViewProfile }: ProfileCardProps) {
    return (
      <div className="bg-white/10 backdrop-blur-md rounded-xl p-6 border border-purple-500/20 hover:border-purple-500/40 transition-all duration-300 hover:transform hover:scale-105">
        {/* Bot Avatar/Icon */}
        <div className="w-16 h-16 bg-gradient-to-br from-purple-500 to-pink-500 rounded-full mx-auto mb-4 flex items-center justify-center">
          <span className="text-2xl">🤖</span>
        </div>
        
        {/* Bot Name */}
        <h3 className="text-xl font-bold text-white text-center mb-2">
          {profile.name}
        </h3>
        
        {/* Self-Description */}
        <p className="text-purple-200 text-sm text-center mb-4 italic">
          "{profile.description}"
        </p>
        
        {/* Core Stats */}
        <div className="grid grid-cols-2 gap-4 mb-4">
          <div className="text-center">
            <div className="text-xs text-purple-300 uppercase">LLM</div>
            <div className="text-sm font-semibold text-white">{profile.llm.primary}</div>
          </div>
          <div className="text-center">
            <div className="text-xs text-purple-300 uppercase">Harness</div>
            <div className="text-sm font-semibold text-white">{profile.harness}</div>
          </div>
        </div>
        
        {/* Skills and Capabilities */}
        <div className="space-y-2">
          <div className="flex flex-wrap gap-1">
            {profile.skills.slice(0, 3).map((skill, index) => (
              <span 
                key={index}
                className="px-2 py-1 bg-purple-500/20 text-purple-200 text-xs rounded-full"
              >
                {skill}
              </span>
            ))}
            {profile.skills.length > 3 && (
              <span className="px-2 py-1 bg-purple-500/20 text-purple-200 text-xs rounded-full">
                +{profile.skills.length - 3}
              </span>
            )}
          </div>
        </div>
        
        {/* Action Button */}
        <button
          onClick={onViewProfile}
          className="w-full mt-4 bg-gradient-to-r from-purple-500 to-pink-500 text-white py-2 px-4 rounded-lg hover:from-purple-600 hover:to-pink-600 transition-colors duration-200"
        >
          View Profile
        </button>
      </div>
    );
  }
  ```
  
  2. Create src/components/HeroSection.tsx with:
     - Introduction to BotArena
     - Call-to-action for CLI tool usage
     - Gaming-inspired visual elements
  
  3. Create src/components/FeaturedProfiles.tsx with:
     - Grid layout for displaying multiple bot profiles
     - Responsive design for mobile and desktop
     - Sample data for demonstration
  
  4. Add TypeScript interfaces for component props
  5. Implement hover states and micro-interactions
  
  From PROJECT.md: Create beautiful, comparable format with gaming-inspired visuals that tap into emotional attachment.
  </action>
  <verify>Components render correctly with sample data and are fully responsive</verify>
  <done>Profile display components created with gaming-inspired design and responsive layout system</done>
</task>

</tasks>

<verification>
- Next.js development server runs without errors
- Homepage renders with gaming-inspired visual design
- ProfileCard component displays bot information correctly
- Responsive design works on mobile and desktop viewports
- Tailwind CSS classes are properly applied
- TypeScript compilation succeeds with no type errors
</verification>

<success_criteria>
- Next.js application runs successfully on localhost:3000
- Homepage displays BotArena branding and introduction
- ProfileCard component renders bot configuration data beautifully
- Responsive design adapts to different screen sizes
- Gaming-inspired visual theme is consistent throughout
- Components are properly typed with TypeScript interfaces
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-foundation/01-03-SUMMARY.md`
</output>